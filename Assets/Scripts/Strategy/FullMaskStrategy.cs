using UnityEngine;

public class FullMaskStrategy : IObjectActionStrategy
{
    public void DiferentAction(TypeAction context)
    {
        //if (context.TextUI != null)
        //    context.TextUI.text = "Máscara completa";

        //if (context.LeftHand != null && context.GetComponent<MeshFilter>() != null)
        //{
        //    context.LeftHand.GetComponent<MeshFilter>().mesh = context.GetComponent<MeshFilter>().mesh;
        //    context.LeftHand.GetComponent<MeshRenderer>().materials = context.GetComponent<MeshRenderer>().materials;
        //    var leftType = context.LeftHand.GetComponent<TypeAction>();
        //    if (leftType != null) leftType._type = context._type;
        //}
    }
}
